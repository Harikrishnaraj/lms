{
  "name": "@lms/tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@lms/tsconfig": "workspace:*",
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
