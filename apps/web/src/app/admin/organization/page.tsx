import { PortalPlaceholder } from '../../../components/shell/portal-placeholder';

export default function AdminOrganizationPage() {
  return (
    <PortalPlaceholder
      title="Organization overview"
      description="Organization settings, roles and permissions, and the audit log."
    />
  );
}
